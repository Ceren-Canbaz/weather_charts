// The stroke width for the lines in a line chart
const double lineStrokeWidth = 5;

// The stroke width for the grid lines in a chart
const double gridStrokeWidth = 2;

// A list representing the days of the week (Monday to Sunday)
const List<String> dayList = ['Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun'];
